<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <link href="@Url.Content("~/Content/Site.css")" rel="stylesheet" type="text/css" />
</head>

<body>
    <nav>
        <ul class="menu">
            <li>@Html.ActionLink("Home", "Index", "Home")</li>
            <li>@Html.ActionLink("All goals", "All", "Home")</li>
            <li>@Html.ActionLink("Planned goals", "Planned", "Home")</li>
            <li>@Html.ActionLink("Important goals", "Important", "Home")</li>
            <li>@Html.ActionLink("Today's goals", "Todays", "Home")</li>
        </ul>
    </nav>
    @RenderBody()
</body>
</html>